# Dependencies
node_modules/

# Build artifacts (committed bundle in dist/ is the distribution unit — see ROADMAP Phase 1)
# dist/  ← intentionally NOT ignored; we ship a built bundle to enable `npx -y github:user/repo`

# Secrets — NEVER commit
token.json
yandex-mail-mcp-desktop/token.json
**/token.json
.env
.env.*
*.pem
*.key

# Audit logs (created at runtime by Phase 5)
audit.jsonl
*.audit.log
.planning/audit/

# Local allowlist (Phase 5)
allowlist.json
**/allowlist.json

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo

# Plan-check temporary output
**/dist/__check__/

# Phase 6 audit log + rotation file (runtime, never committed)
audit.jsonl.1
**/audit.jsonl.1
pending-trust.json
**/pending-trust.json
secret.bin
**/secret.bin

# Layer 1.5 runtime state (never committed)
risk-policy.json
**/risk-policy.json
recent-sends.jsonl
**/recent-sends.jsonl
override-tokens.jsonl
**/override-tokens.jsonl

# Внутренние рабочие материалы — не для публики
.planning/
CLAUDE.md
MILESTONE-DEEP-REVIEW-PROMPT.md
.github/REPO-SETUP.md
