{
  "name": "yandex-mail-mcp-desktop",
  "version": "1.0.0",
  "description": "MCP server for Yandex Mail — Claude Desktop (stdio)",
  "main": "dist/index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "esbuild src/index.ts src/tools.ts src/imap.ts src/smtp.ts src/token.ts --bundle=false --platform=node --target=node18 --outdir=dist --format=cjs",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "imapflow": "^1.0.162",
    "mailparser": "^3.7.1",
    "nodemailer": "^6.9.8",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/nodemailer": "^6.4.14",
    "esbuild": "^0.21.0",
    "typescript": "^5.9.3"
  }
}
